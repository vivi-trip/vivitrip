import SideNavigationMenu from "@/src/components/SideNavigationMenu/SideNavigationMenu";

const MyProfile = () => {
  const onClick = () => {
    
  };
  return (
    <div>
      <SideNavigationMenu url={onClick} />
    </div>
  );
};

export default MyProfile;
